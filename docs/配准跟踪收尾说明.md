# 配准与目标跟踪收尾说明

更新时间：2026-03-24

## 本次收尾完成项

### 遥感影像配准

- 复核了前端注册页、后端注册接口、历史记录入库字段之间的契约一致性。
- 补齐历史页对配准附加结果的消费入口：
  - 可继续查看 `overlay_path` 对应的叠加图
  - 可继续查看 `checkerboard_path` 对应的棋盘复核图
- 主结果下载逻辑保持不变，仍以下历史记录的 `after_img` 作为默认下载对象。

### 遥感目标跟踪

- 复核了前端跟踪页、后端跟踪接口、历史记录元数据之间的契约一致性。
- 补齐历史页对轨迹 JSON 的消费入口：
  - 可继续打开 `trajectory_path` 对应的轨迹文件
- 历史页原有的视频打开与视频下载能力保持可用。

## Docker / 依赖结论

- 本次收尾仅涉及历史页资产消费与文档同步，没有新增运行时依赖。
- `backend/requirements.txt` 与 `backend/requirements-hf.txt` 在前两次本地提交里已经完成配准/跟踪所需同步，本次不需要继续修改。
- `Dockerfile` 也不需要继续调整：
  - 主服务环境已经通过 `backend/requirements.txt` 安装依赖
  - `HFPyTorch310` 环境中原本已安装 Kornia 相关组件
  - 本次收尾没有新增服务、端口、卷或环境变量

## 当前仍保留的边界

- 配准历史页已能查看附加复核图，但默认下载仍只下载主配准结果。
- 跟踪历史页已能打开轨迹 JSON，但还没有做轨迹可视化。
- 两项能力仍属于工程基线，不等同于完整遥感生产级流水线。
