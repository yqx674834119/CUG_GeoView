# 本地交付镜像验收测试报告

## 1. 测试目标

本次验收面向最终交付产物，验证以下内容：

1. 后端整项目镜像可独立运行，不依赖容器外代码目录。
2. 前端镜像可独立运行，不依赖容器外前端代码目录。
3. 前后端分离时，前端可正确访问后端 API 与图片资源。
4. 前端暴露功能在本地 Docker 环境内可跑通，并能正确返回结果资源。
5. 集群默认配置下，图片资源提供备用链路，规避 `ERR_CONTENT_LENGTH_MISMATCH` / `curl: (18)` 类问题。
6. 前端 Helm 包模板、示例值与镜像标签保持一致。

## 2. 交付产物

- 后端镜像：`crpi-4r2gidb79yjyny4o.cn-hangzhou.personal.cr.aliyuncs.com/shawnyao/cugrs:20260428-final`
- 后端镜像 ID：`4d8488bd8cfd`
- 前端镜像：`crpi-4r2gidb79yjyny4o.cn-hangzhou.personal.cr.aliyuncs.com/shawnyao/geoview-frontend:20260428-final`
- 前端镜像 ID：`e86ea13b86d6`
- 前端 Helm 包：`deploy/frontend/helm/dist/geoview-frontend-0.1.1.tgz`

## 3. 配置文件

### 3.1 本地配置

- 后端本地配置：[config.local.yaml](/home/livablecity/GeoView/config.local.yaml:1)
  - `services.backend: true`
  - `services.frontend: false`
  - `assets.serve_mode: buffered`
- 前端本地运行时环境：[docker/frontend.local.env](/home/livablecity/GeoView/docker/frontend.local.env:1)
  - `GEOVIEW_BACKEND_URL=http://127.0.0.1:5008`
  - `GEOVIEW_BACKEND_ASSET_MODE=buffered`

### 3.2 集群默认配置

- 后端集群默认配置：[config.yaml](/home/livablecity/GeoView/config.yaml:1)
  - `services.backend: true`
  - `services.frontend: false`
  - `assets.serve_mode: buffered`
- 前端集群运行时环境：[docker/frontend.cluster.env](/home/livablecity/GeoView/docker/frontend.cluster.env:1)
  - `GEOVIEW_BACKEND_URL=http://172.20.20.240:31839/186/5008`
  - `GEOVIEW_BACKEND_ASSET_MODE=buffered`
- 前端 Helm 示例值：[values-harbor-example.yaml](/home/livablecity/GeoView/deploy/frontend/helm/geoview-frontend/values-harbor-example.yaml:1)
  - 镜像 tag 已更新为 `20260428-final`
  - `backendUrl` 已固定为 `http://172.20.20.240:31839/186/5008`
  - `backendAssetMode` 已固定为 `buffered`

## 4. 关键修复

### 4.1 `mmseg_cugrs`

- 修复位置：
  - [Dockerfile](/home/livablecity/GeoView/Dockerfile:1)
  - [mmseg_cugrs_ms_deform_attn.py](/home/livablecity/GeoView/docker/patches/mmseg_cugrs_ms_deform_attn.py:1)
- 结论：
  - `custom_fwd(..., device_type=...)` 与当前 Torch AMP 版本不兼容的问题已修复。
  - `TestData/Seg/2.tif` 本地容器验证通过。

### 4.2 变化检测孔洞后处理

- 修复位置：[hole.py](/home/livablecity/GeoView/backend/applications/image_processing/hole.py:1)
- 结论：
  - `res/hole/` 目录缺失导致的 `cv2.imwrite` 失败已修复。
  - `TestData/CD/Val1/val_1.png` + `TestData/CD/Val2/val_1.png` 本地容器验证通过。

### 4.3 配准 `loftr_outdoor`

- 修复位置：[hf_registration.py](/home/livablecity/GeoView/backend/applications/interface/hf_registration.py:1)
- 结论：
  - 已移除对 `K.io.load_image` / `kornia_rs` IO API 的硬依赖。
  - 最终镜像中 `registration/loftr_outdoor` 已通过本地容器验证。

### 4.4 图片资源备用链路

- 修复位置：
  - [file.py](/home/livablecity/GeoView/backend/applications/api/file.py:1)
  - [config.py](/home/livablecity/GeoView/backend/applications/configs/config.py:1)
  - [backendAssetUrl.js](/home/livablecity/GeoView/frontend/src/utils/backendAssetUrl.js:1)
  - [frontend-entrypoint.sh](/home/livablecity/GeoView/docker/frontend-entrypoint.sh:1)
- 结论：
  - 默认资源模式已统一为 `buffered`。
  - 图片走 `/api/file/assets-buffered/photos/<path>` 备用链路。
  - 视频走应用层 `Range` 流式返回，避免依赖旧的文件直出行为。
  - 前端可通过 `GEOVIEW_BACKEND_ASSET_MODE=buffered` 自动切换到图片备用链路。

### 4.5 官方 BoT-SORT 初始框判定修复

- 修复位置：
  - [tracking.py](/home/livablecity/GeoView/backend/applications/interface/tracking.py:1)
- 结论：
  - `botsort_official` 之前被后端错误判定为“必须提供初始框”，与前端行为不一致。
  - 现已统一为：
    - `botsort`
    - `botsort_official`
    都不需要初始框。
  - 该修复已经固化进后端镜像 `cugrs:20260428-final`，并完成容器级复测。

### 4.6 视频输入预览标准化

- 修复位置：
  - [file.py](/home/livablecity/GeoView/backend/applications/api/file.py:1)
  - [upload.js](/home/livablecity/GeoView/frontend/src/api/upload.js:1)
  - [Tracking.vue](/home/livablecity/GeoView/frontend/src/views/mainfun/Tracking.vue:1)
- 结论：
  - 浏览器本地 `blob:` 预览只要命中不兼容编码就会失败，这不是后端算法问题，而是浏览器解码限制。
  - 现已新增 `/api/file/upload-video-preview`，前端在选择单视频后会先生成标准化 MP4 预览，并复用该上传结果执行后续跟踪。
  - 本地浏览器级回归已确认：
    - 原始本地 `blob:` 预览失败后，页面会切换到后端标准化预览。
    - 标准化预览与结果视频都能被浏览器实际播放。
    - 资源链路仍走 `buffered` URL，但视频响应为应用层 `206 Range`，不再触发 `ERR_CONTENT_LENGTH_MISMATCH`。

## 5. 本地验收结果

### 5.1 前后端分离本地运行

- 状态：PASS
- 前端容器：
  - `http://127.0.0.1:3100`
  - `runtime-config.js` 返回 `backendUrl=http://127.0.0.1:5008`
  - `backendAssetMode=buffered`
- 后端容器：
  - `http://127.0.0.1:5008`
  - SQLite 路径：`/app/backend/static/geoview.sqlite3`
  - 上传目录：`/app/backend/static/upload`
  - 结果目录：`/app/backend/static/upload/res`
  - GPU 检测：`Detected 1 GPU(s).`
- 独立性：
  - `docker inspect geoview-backend-test geoview-frontend-test` 显示 `HostConfig.Binds=null`
  - 说明当前验收容器未挂载宿主机代码目录

### 5.2 变化检测

- 状态：PASS
- 数据：
  - `TestData/CD/Val1/val_1.png`
  - `TestData/CD/Val2/val_1.png`
- 模型：
  - `backend/model/change_detection/bit_256x256`
- 验证结果：
  - `/api/analysis/change_detection` 返回成功。
  - 历史记录计数：`1`
  - 结果图与附加结果图均可通过 `/api/file/assets/photos/...` 完整取回。

### 5.3 语义分割 / 地物分类

- 状态：PASS
- 数据：
  - `TestData/Seg/2.tif`
- 模型：
  - `backend/model/semantic_segmentation/mmseg_cugrs`
  - `backend/model/semantic_segmentation/paddle_deeplabv3p`
- 验证结果：
  - 两个模型均返回成功。
  - 历史记录计数：`2`
  - 结果图可完整取回。

### 5.4 目标检测

- 状态：PASS
- 数据：
  - `TestData/Dec/aircraft_4.jpg`
- 模型：
  - `backend/model/object_detection/hf_conditional_detr_resnet50`
  - `backend/model/object_detection/hf_detr_resnet50`
  - `backend/model/object_detection/hf_waldo30`
  - `backend/model/object_detection/mmrotate_oriented_rcnn_r50_fpn_1x_dota_le90`
  - `backend/model/object_detection/paddle_yolo`
- 验证结果：
  - 五个前端可见目标检测模型均返回成功。
  - 历史记录计数：`5`
  - 检测结果图可完整取回。

### 5.5 场景分类

- 状态：PASS
- 数据：
  - `TestData/Dec/aircraft_4.jpg`
- 模型：
  - `backend/model/classification/resnet50`
- 验证结果：
  - 接口返回成功。
  - 历史记录计数：`1`
  - 历史记录结果示例：`{"storagetanks": 0.54232}`

### 5.6 图像超分

- 状态：PASS
- 数据：
  - `TestData/Dec/aircraft_4.jpg`
- 模型：
  - `backend/model/image_restoration/hf_swin2sr_x2`
- 验证结果：
  - 接口返回成功。
  - 历史记录计数：`1`
  - 超分结果图可完整取回。

### 5.7 目标跟踪

- 状态：PASS
- 数据：
  - `TestData/Tracking/official_mot17_02_frcnn_180frames_raw.mp4`
- 模型：
  - `backend/model/tracking/botsort`
  - `backend/model/tracking/botsort_official`
- 验证结果：
  - 前端可见跟踪模型为两个：工程版 `botsort` 和官方版 `botsort_official`。
  - 源码级模型列表检查返回：
    - `backend/model/tracking/botsort`
    - `backend/model/tracking/botsort_official`
  - 容器级接口复测返回：
    - `PASS backend/model/tracking/botsort`
    - `PASS backend/model/tracking/botsort_official`
  - 预览图、结果视频、轨迹 JSON 均可完整取回。
  - 浏览器级复测返回：
    - 输入视频预览最终走标准化 MP4 预览
    - 结果视频可直接播放

### 5.8 自动配准

- 状态：PASS
- 功能验证数据：
  - 使用同一张 `TestData/Dec/aircraft_4.jpg` 生成同图像对，验证接口链路与运行时能力。
- 模型：
  - `backend/model/registration/auto`
  - `backend/model/registration/loftr_outdoor`
  - `backend/model/registration/opencv`
- 验证结果：
  - 三个前端可见配准模型均返回成功。
  - `registration/auto` 与 `registration/loftr_outdoor` 最终都走通 `kornia_loftr_external`
  - `registration/opencv` 返回 `opencv_orb`
  - 三个模型的 `registered / overlay / checkerboard` 结果图均可完整取回。

### 5.9 集群默认图片链路备用方案

- 状态：PASS
- 验证方式：
  - 后端以 [config.yaml](/home/livablecity/GeoView/config.yaml:1) 启动，`assets.serve_mode=buffered`
  - 前端以 [frontend.cluster.env](/home/livablecity/GeoView/docker/frontend.cluster.env:1) 启动，`backendAssetMode=buffered`
- 验证结果：
  - `runtime-config.js` 返回：
    - `backendUrl=http://172.20.20.240:31839/186/5008`
    - `backendAssetMode=buffered`
  - 图片直接链路 `/api/file/assets/photos/...` 返回：
    - `Content-Length=208387`
    - 实际下载长度 `208387`
  - 备用链路 `/api/file/assets-buffered/photos/...` 返回：
    - `Content-Length=208387`
    - 实际下载长度 `208387`
  - 两条链路都返回：
    - `Access-Control-Allow-Origin: http://172.20.20.240:31839`
    - `Cache-Control: no-cache`

### 5.10 视频范围请求与长度一致性

- 状态：PASS
- 验证方式：
  - 使用 Flask `test_client()` 直接请求后端资源接口。
  - 图片测试：
    - `GET /api/file/assets/photos/probe.png`
  - 视频测试：
    - `GET /api/file/assets/photos/probe.mp4` with `Range: bytes=0-127`
    - `GET /api/file/assets-buffered/photos/probe.mp4` with `Range: bytes=128-255`
- 验证结果：
  - 图片返回：
    - `HTTP 200`
    - `Content-Length=13`
    - 实际读取长度 `13`
  - 视频主链路返回：
    - `HTTP 206`
    - `Accept-Ranges: bytes`
    - `Content-Range: bytes 0-127/4096`
    - `Content-Length=128`
    - 实际读取长度 `128`
  - 视频备用链路返回：
    - `HTTP 206`
    - `Accept-Ranges: bytes`
    - `Content-Range: bytes 128-255/4096`
    - `Content-Length=128`
    - 实际读取长度 `128`
  - 结论：
    - 当前后端实现对图片和视频都能返回一致的响应体长度。
    - 这消除了应用层“声明长度与实际返回长度不一致”的已知风险点。

### 5.11 自包含与宿主机路径检查

- 状态：PASS
- 验证方式：
  - 检查运行时关键目录：`backend/applications`、`backend/model`、`frontend/src`、`docker`、`deploy`
  - 搜索是否仍存在固定宿主机运行依赖：
    - `/home/livablecity/miniconda3/envs`
    - `/home/livablecity/geoview_runtime`
- 验证结果：
  - 运行时关键目录中未检出上述固定宿主机路径残留。
  - `MMSeg310` 与 `HFPyTorch310` 调用逻辑现为：
    - 优先使用镜像内 `/opt/conda/envs/.../bin/python`
    - 回退到当前用户家目录下的 `~/miniconda3/envs/.../bin/python`
    - 最后回退到 `conda run -n ...`
  - `botsort_official` 运行目录已随项目收纳到：
    - `backend/runtime/BoT-SORT`
  - 结论：
    - 当前源码已满足“运行时不依赖固定宿主机目录”的自包含要求。
    - 当前验收容器也未挂载宿主机代码目录，运行时独立性验证通过。

### 5.12 浏览器级视频预览与播放回归

- 状态：PASS
- 验证方式：
  - 使用 Playwright 打开 `http://127.0.0.1:3100/#/tracking`
  - 上传 `TestData/Tracking/official_mot17_02_frcnn_180frames_raw.mp4`
  - 观察前端是否能从本地 `blob:` 失败自动切换到后端标准化预览
  - 执行 `botsort_official` 跟踪，并校验输入预览视频与结果视频是否都进入可播放状态
- 验证结果：
  - 前端模型单选实际显示 2 个：
    - `BoT-SORT 官方原始版（ReID）`
    - `BoT-SORT 工程集成版（Ultralytics + WALDO30）`
  - `/api/file/upload-video-preview` 返回成功
  - 输入视频标准化预览 URL：
    - `/api/file/assets-buffered/photos/res/fe93d43b64dcefcea088bea9a41f0966_tracking_official_mot17_02_frcnn_180frames_raw_source_preview.mp4`
  - 浏览器对输入预览视频的实际状态：
    - `readyState=4`
    - `videoWidth=1920`
    - `videoHeight=1080`
    - `duration=30`
  - 跟踪完成后，浏览器对结果区两个视频的实际状态均为：
    - `readyState=4`
    - `error=null`
  - 浏览器请求视频资源时返回：
    - `HTTP 206`
    - `Accept-Ranges: bytes`
    - `Content-Range` 合法
  - Playwright 观察到的 `net::ERR_ABORTED` 出现在浏览器主动取消旧 Range 分片请求时，不是服务端截断；最终播放状态为成功。

## 6. Helm 验证

- 状态：PASS（交付包已生成，本机未执行 `helm lint`）
- 验证结果：
  - Chart 元数据：
    - `version: 0.1.1`
    - `appVersion: 20260428-final`
  - Harbor 示例值：
    - 前端镜像 `172.20.20.107:8443/your-project/geoview-frontend:20260428-final`
    - `backendUrl=http://172.20.20.240:31839/186/5008`
    - `backendAssetMode=buffered`
  - 由于本机没有 `helm` 命令，且临时拉取 `alpine/helm` 镜像时遇到 Docker Hub EOF，本轮未执行 `helm lint/template`
  - 已直接按 Helm Chart 目录结构打包生成交付包
- 产物：
  - `deploy/frontend/helm/dist/geoview-frontend-0.1.1.tgz`

## 6.1 模型列表问题专项诊断

- 现象：
  - 本地前端打开后，模型列表请求可能失败，页面表现为“未检测到模型文件/配置”或初始化模型单选项为空。
- 已定位原因：
  - 前端模型列表接口固定请求 `/api/model/list/<type>`。
  - 当浏览器侧没有拿到有效的 `runtime-config.js`，且前端不是通过正确的运行时注入方式启动时，前端会回退到错误或不稳定的后端地址。
  - 旧版 [runtime-config.js](/home/livablecity/GeoView/frontend/public/runtime-config.js:1) 只写空对象，回退行为不够稳健。
  - 本地 `vue` 开发服务原先也没有 `/api` 代理，导致开发态排障成本高。
- 已修复：
  - [runtime-config.js](/home/livablecity/GeoView/frontend/public/runtime-config.js:1) 已补充默认值，默认回退到当前主机 `5008` 端口。
  - [global.vue](/home/livablecity/GeoView/frontend/src/global.vue:14) 已补充最终兜底地址 `http://127.0.0.1:5008/`。
  - [vue.config.js](/home/livablecity/GeoView/frontend/vue.config.js:1) 已增加本地 `/api` 代理，并优先读取 [config.local.yaml](/home/livablecity/GeoView/config.local.yaml:1)。
- 对集群的影响判断：
  - 如果前端容器通过镜像默认 `nginx` 入口启动，并正确注入 `GEOVIEW_BACKEND_URL`，则集群不会命中这个问题。
  - 如果服务部署平台覆盖了镜像默认入口，或者没有给前端注入 `GEOVIEW_BACKEND_URL`，集群会出现相同问题，且由于集群后端地址带路径前缀 `/186/5008`，问题会比本地更明显。
- 集群修复要求：
  - 前端服务部署必须保留镜像默认启动方式，确保 [frontend-entrypoint.sh](/home/livablecity/GeoView/docker/frontend-entrypoint.sh:1) 能生成 `runtime-config.js`。
  - 前端服务部署必须显式设置：
    - `GEOVIEW_BACKEND_URL=http://172.20.20.240:31839/186/5008`
    - `GEOVIEW_BACKEND_ASSET_MODE=buffered`

## 6.2 本轮手工复测问题修复

- 变化检测 `mask` 前端缺失：
  - 根因不是算法没出结果，而是后端把 `mask` 和 `mask_hole` 存成了容器内文件系统绝对路径，前端无法直接当成可访问资源地址使用。
  - 修复：
    - [backend/applications/interface/analysis.py](/home/livablecity/GeoView/backend/applications/interface/analysis.py:104) 改为存储 `/api/file/assets/photos/res/...` 风格的 API 资源路径。
    - [frontend/src/utils/backendAssetUrl.js](/home/livablecity/GeoView/frontend/src/utils/backendAssetUrl.js:1) 增加对旧绝对路径 `/app/backend/static/upload/...` 的兼容改写。

- 目标跟踪结果视频无法显示：
  - 根因是集群默认 `buffered` 资源模式更适合图片，但视频更依赖浏览器的原生流式/范围请求能力。
  - 修复：
    - [backend/applications/api/file.py](/home/livablecity/GeoView/backend/applications/api/file.py:17) 对 `video/*` 资源强制走直接文件流，不再走 `buffered` 内存回包。

- 目标跟踪缺少“原版”模型：
  - 根因是官方 BoT-SORT 模型列表之前额外受环境变量开关限制，即使本机已有运行目录也不会暴露。
  - 修复：
    - [backend/applications/api/model.py](/home/livablecity/GeoView/backend/applications/api/model.py:92) 现在只要 `official_repo_dir` 实际存在，就会暴露 `botsort_official`。
    - [backend/applications/interface/tracking.py](/home/livablecity/GeoView/backend/applications/interface/tracking.py:497) 运行时校验也改为“目录存在即可运行”。
  - 自包含处理：
    - 官方 BoT-SORT 运行目录已收纳到项目内 `backend/runtime/BoT-SORT`。
    - `backend/model/tracking/botsort_official/model_manifest.json` 已改为指向项目内运行目录。
    - 源码级模型列表检查已返回 `botsort` 与 `botsort_official` 两项。

## 7. 结论

- 后端镜像 `cugrs:20260428-final`：可交付
- 前端镜像 `geoview-frontend:20260428-final`：可交付
- 前端 Helm 包 `geoview-frontend-0.1.1.tgz`：可交付
- 默认集群配置已包含：
  - 图片 `buffered` 备用链路
  - 视频 `Range` 流式返回
  - 单视频输入标准化预览链路

## 7.1 当前残余说明

- 本机当前没有 `helm` 命令，因此本轮未执行原生 `helm lint/template`
- 当前 Helm 交付包已生成，可直接交付；如需在正式发版机上补做 `helm lint`，建议在具备 `helm` 的环境执行一次

## 8. 配准基准数据补充

本次本地验收已经验证配准接口和运行时能力，但如果需要对 `LoFTR` 做真实场景基准测试，建议补充以下公开数据集：

- HPatches：<https://github.com/hpatches/hpatches-dataset>
- MegaDepth：<https://www.cs.cornell.edu/projects/megadepth/>
- Kornia LoFTR 文档：<https://kornia.readthedocs.io/en/latest/models/loftr.html>
